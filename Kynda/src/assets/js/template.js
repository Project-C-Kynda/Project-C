/*
// This function will handle all the changes that will be made on the template.
function updateTemplate() 
{
    var titleText = document.getElementById("header-text");
    var middleText = document.getElementById("middle-text");
    var bottomText = document.getElementById("bottom-text");

    var templateTitleInput = document.getElementById("template-title").value;
    var templateMiddleInput = document.getElementById("template-text").value;
    var templateBottomInput = document.getElementById("template-textfoto").value;

    if(titleText.value != templateTitleInput && templateTitleInput !== "")
    {
        titleText.innerHTML = templateTitleInput;
    }

    if(middleText.value != templateMiddleInput && templateMiddleInput !== "") 
    {
        middleText.innerHTML = templateMiddleInput;
    }

    if(bottomText.value != templateBottomInput && templateBottomInput !== "") 
    {
        bottomText.innerHTML = templateBottomInput;
    }
}
*/